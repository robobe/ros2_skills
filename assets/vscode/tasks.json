{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "colcon",
      "detail": "Build all ros2 packages using colcon",
      "type": "shell",
      "command": "bash",
      "args": [
        "-lc",
        "source /opt/ros/jazzy/setup.bash && if [ -f install/setup.bash ]; then source install/setup.bash; fi && colcon build"
      ],
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "problemMatcher": [],
      "group": {
        "kind": "build",
        "isDefault": true
      }
    }
  ]
}